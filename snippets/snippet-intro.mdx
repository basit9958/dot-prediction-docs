All data endpoints require an API key. Pass it using the `X-API-Key` header or `Authorization: Bearer` header with every request. See the [Authentication guide](/guides/authentication) for setup instructions.
