---
title: Quickstart
icon: "rocket"
description: Get up and running with the Dot Prediction API in minutes.
---

<Steps>

<Step title="Register">
```bash
curl -X POST https://api.dotprediction.io/v1/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name": "Your Name", "email": "you@example.com", "password": "your-password"}'
```
</Step>

<Step title="Create an API key">
```bash
curl -X POST https://api.dotprediction.io/v1/api/auth/api-keys \
  -H "Content-Type: application/json" \
  -b "jwt=YOUR_SESSION_COOKIE" \
  -d '{"name": "my-first-key"}'
```
Save the `key` from the response -- it cannot be retrieved again.
</Step>

<Step title="Fetch markets">
<CodeGroup>
```bash curl
curl https://api.dotprediction.io/v1/api/markets?provider=polymarket&limit=5 \
  -H "X-API-Key: YOUR_API_KEY"
```

```python Python
import requests
resp = requests.get("https://api.dotprediction.io/v1/api/markets",
    params={"provider": "polymarket", "limit": 5},
    headers={"X-API-Key": "YOUR_API_KEY"})
```

```javascript JavaScript
const res = await fetch(
  "https://api.dotprediction.io/v1/api/markets?provider=polymarket&limit=5",
  { headers: { "X-API-Key": "YOUR_API_KEY" } });
const { markets } = await res.json();
```

</CodeGroup>
</Step>

</Steps>
